name: karma_quest
description: A gamified task manager based on karmic principles.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.6.0
  provider: ^6.1.2
  http: ^1.2.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  google_generative_ai: ^0.4.0
  shared_preferences: ^2.2.3

flutter:
  uses-material-design: true

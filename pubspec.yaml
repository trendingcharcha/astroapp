name: astroapp
description: CosmoVedic - Premium Vedic Astrology & KarmaQuest App
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.7.0
  flutter_local_notifications: ^17.1.0
  permission_handler: ^11.3.1
  timezone: ^0.9.2
  url_launcher: ^6.3.0          # Fix 2: Chrome Custom Tab for Google OAuth

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - manifest.json
